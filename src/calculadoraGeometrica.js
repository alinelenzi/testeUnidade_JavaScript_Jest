let calcularAreaQuadrado = (lado) => lado**2;

let calcularAreaParalelogramo = (base, altura) => base * altura;

let calcularAreaTriangulo = (base, altura) => base * altura / 2;

module.exports = {
    calcularAreaQuadrado,
    calcularAreaParalelogramo,
    calcularAreaTriangulo

}